[project]
name = "macquerel"
version = "0.1.0"
description = "Quantum state-vector simulator for Apple Silicon"
authors = ["Guen Prawiroatmodjo <4041805+guenp@users.noreply.github.com>"]

[project.dependencies]
python = "^3.11"
mlx = "^0.3.0"
pytest = "^7.4.0"

[build-system]
requires = ["uv"]
build-backend = "uv.build.backend"