# macquerel benchmarks moved

The maintained benchmark suite now lives under `benchmarks/`.

Use:

```bash
uv run python benchmarks/bench_statevector.py
uv run python benchmarks/bench_fusion_width.py
uv run python benchmarks/bench_versions.py --versions latest
```
